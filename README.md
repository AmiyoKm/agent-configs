# Agent configs

Personal configuration for Claude Code, Codex, opencode, and local agent skills.

This repo tracks portable config only. It excludes auth files, histories, caches, shell snapshots, SQLite state, logs, lock files, paste caches, and installed dependencies.

The opencode TokenRouter API key is read from `TOKENROUTER_API_KEY`.
